---
"@narumitw/pi-accounts": minor
---

Add a portable `Ctrl+Alt+A` selector, block account changes during active runs, and seed genuinely new sessions from the newest usable same-project account selection.
